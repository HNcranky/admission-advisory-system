\documentclass[../main.tex]{subfiles}
\begin{document}

% Target length: 17-20 pages.

Chapter~\ref{chapter:Methodology} reviewed the technologies the system is built on.
This chapter presents how they are assembled into a working system and how the result is evaluated.
Section~\ref{section:4.1} covers the architecture and package design, Section~\ref{section:4.2} the user interface, layer interactions, and database, Section~\ref{section:4.3} the implementation libraries and measured achievements, Section~\ref{section:4.4} the testing strategy and its results, and Section~\ref{section:4.5} the deployment.

\section{Architecture design}
\label{section:4.1}

\subsection{Software architecture selection}

The system is built as a \textit{layered, service-oriented monolith}: a single deployable application internally organized into layers (web, services, advisory pipeline, ingestion, database) with explicit boundaries between them.
A microservice decomposition was considered and rejected.
The functional requirements form one cohesive domain --- a student message flows through routing, profile collection, retrieval, reasoning, and explanation in a single logical transaction --- and splitting these stages across network boundaries would add serialization, partial-failure handling, and deployment complexity without a corresponding scaling benefit at the system's scale.
A monolith also keeps the vector corpus, the canonical admission records, and the chat state in one PostgreSQL instance, which the conflict-tolerance requirement (NFR-02) relies on, since conflict detection queries structured records and \gls{RAG} queries vector chunks against the same database.
The layered internal structure preserves most of the benefits a service split would have offered --- clear interfaces, independent testability, single-responsibility modules --- without the distributed-systems cost.

\subsection{Overall design}

Figure~\ref{fig:package-overview} shows the package structure and the principal dependencies between packages.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{package_overview.png}
    \caption{Package overview and dependencies between layers}
    \label{fig:package-overview}
\end{figure}

The \texttt{web} package is the entry layer: it exposes the chat \gls{HTTP} \gls{API} and serves the interface.
The \texttt{services} package holds the domain logic, divided into focused sub-packages --- \texttt{chat} (session, routing, dispatch), \texttt{inference} (the \gls{LLM} gateway), \texttt{knowledge} (\gls{RAG}), \texttt{conflict}, \texttt{cutoff}, \texttt{profile}, and \texttt{tracing}.
The advisory pipeline (\texttt{graph.py}, \texttt{state.py}, and the \texttt{agents} package) is the orchestration layer driven by the chat dispatchers.
The \texttt{ingestion} package is an independent layer feeding the database offline.
All layers converge on a single PostgreSQL instance.
Dependencies point inward and downward: the web layer depends on services, services depend on the inference gateway and the database, and no lower layer depends on the web layer.

\subsection{Detailed package design}

Three packages carry the core logic and are described in detail.

\textbf{Advisory pipeline.}
The pipeline is defined in \texttt{graph.py} as a LangGraph state graph over the shared state \texttt{state.py::AgentState}.
That single Pydantic model threads the entire run: it carries the parsed \texttt{student\_profile} and \texttt{retrieval\_filters} the early stages write, the \texttt{retrieved\_programs}, \texttt{conflict\_records}, and \texttt{resolution\_outcomes} the middle stages attach, the \texttt{eligibility\_checks} and \texttt{ranked\_recommendations} the reasoning stage produces, and a \texttt{trace\_run\_id} that ties every stage's event to one run.
Six nodes are registered and connected in a fixed linear order --- \texttt{profile}, \texttt{retrieve}, \texttt{conflict}, \texttt{reason}, \texttt{policy}, \texttt{explain} --- as shown in Figure~\ref{fig:advisory-graph} and in the wiring excerpt of Listing~\ref{lst:graph}.
Each node is a thin function in the \texttt{agents} package that reads fields from the state, calls a service for the real work, writes its results back, and returns the state; for example, the reasoning node calls \texttt{reason\_candidates} and stores the resulting eligibility checks and ranked recommendations.
Every node is wrapped by a \texttt{traced(...)} decorator (from \texttt{services/tracing}) that records a per-stage event, so the fixed structure of the graph is exactly what makes the pipeline observable: because the edges are static rather than chosen at runtime, the stage sequence is identical on every run and each stage maps to one predictable trace event.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{advisory_graph.png}
    \caption{The advisory pipeline as a fixed LangGraph state graph}
    \label{fig:advisory-graph}
\end{figure}

The wiring, reproduced verbatim from \texttt{graph.py} in Listing~\ref{lst:graph}, makes the determinism explicit: each \texttt{add\_node} call wraps an agent function in the \texttt{traced(stage, sequence, extractor)} decorator before registering it, and the six \texttt{add\_edge} calls fix the order, leaving no conditional branch for an \gls{LLM} to choose at runtime.

\begin{lstlisting}[language=Python,caption={Advisory pipeline wiring in \texttt{graph.py}: each node is registered through the tracing decorator and the edges fix a linear order},label=lst:graph]
builder.add_node("profile",  traced("profile",  0, extract_profile)(profile_agent))
builder.add_node("retrieve", traced("retrieve", 1, extract_candidates)(retrieval_agent))
builder.add_node("conflict", traced("conflict", 2, extract_conflicts)(conflict_agent))
builder.add_node("reason",   traced("reason",   3, extract_reasoning)(reasoning_agent))
builder.add_node("policy",   traced("policy",   4, extract_policy)(policy_agent))
builder.add_node("explain",  traced("explain",  5, extract_explanation)(explanation_agent))

builder.set_entry_point("profile")
builder.add_edge("profile", "retrieve")
builder.add_edge("retrieve", "conflict")
builder.add_edge("conflict", "reason")
builder.add_edge("reason", "policy")
builder.add_edge("policy", "explain")

graph = builder.compile()
\end{lstlisting}

\textbf{Inference gateway.}
All \gls{LLM} access is funneled through \texttt{services/inference/gateway.py}.
A call site builds an \texttt{InferenceRequest} and receives an \texttt{InferenceResult}; the gateway resolves a per-agent policy from the model \texttt{registry.py} (primary model, optional fallback model, retry budget) and delegates the actual call to a provider in \texttt{providers/gemini\_provider.py}.
The policy fields are resolved by the calling agent's name, so a high-volume step such as intent classification can run with \texttt{allow\_fallback} off and degrade to deterministic keywords, while a harder-to-reconstruct step can be granted a secondary model --- the resilience budget is allocated per call site rather than globally.
This single choke point is where the retry, fallback, and failure-type logic of Section~\ref{section:5.2} lives, so every reasoning step in the system inherits the same resilience without repeating it.

\textbf{Ingestion pipeline.}
The ingestion layer is orchestrated by \texttt{ingestion/pipeline/ingestion\_pipeline.py}, which runs each source through five steps: fetch (dispatched by strategy), route (predict the document type), parse (dispatch the matching parser), extract (pull admission facts from parsed text and tables), and normalize (map raw facts to canonical records using per-school dictionaries).
Normalized records are persisted by \texttt{storage/db\_writer.py}, which upserts each record with an \texttt{ON CONFLICT} key that includes the source \gls{URL} (\texttt{(school\_id, admission\_year, program\_id, admission\_method, source\_url)}), so a re-fetch refreshes the same publication in place while two officially disagreeing sources remain two distinct rows.
The pipeline can run for one source, for all sources of a school, or for an ad-hoc \gls{URL}, matching the operator use cases of Section~\ref{subsection:2.2.2}.

\section{Detailed design}
\label{section:4.2}

\subsection{User interface design}

The interface is a single-page chat application with a three-pane layout, shown in Figure~\ref{fig:ui-landing}.
The center pane is the conversation.
The left pane shows the profile the system has collected so far and the latest recommendation, making the incremental profile collection visible to the student.
The right pane is the advisory analysis panel: it lists the six pipeline stages and, when the operator debug mode is enabled, shows each stage moving from \textit{pending} to \textit{running} to \textit{completed} as the background run progresses.
This panel is the user-facing surface of the tracing subsystem and realizes the operator observability use case.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{screenshot_chat_landing.png}
    \caption{The chat interface: profile pane (left), conversation (center), and the six-stage advisory analysis panel (right)}
    \label{fig:ui-landing}
\end{figure}

% TODO: add a second screenshot (screenshot_chat_conversation.png) of a
% completed advisory consultation showing ranked recommendations and a conflict
% note. This requires a live run against the Gemini API (consumes quota) and
% must be captured by the author; reference it from this subsection once added.

\subsection{Layer design}

The interaction between layers is clearest in the two central flows.

\textbf{Advisory dispatch.}
Figure~\ref{fig:seq-advisory} traces a student message that triggers an advisory run, and Algorithm~\ref{alg:dispatch} gives the same flow as control flow across the request thread and the background worker.
The message endpoint in the \texttt{chat\_api} route calls the conversation service, which classifies intent, updates the profile, and decides whether to start a run.
When a run is warranted, \texttt{start\_run} inserts a row into \texttt{chat\_advisory\_runs} in the \textit{queued} state, recording the dispatch arguments and a profile snapshot, and the endpoint returns immediately so the request does not block (NFR-04).
The work is drained off that queue by a separate \texttt{RunQueueWorker}: it polls for the oldest queued run and claims it atomically with an \texttt{UPDATE \ldots FOR UPDATE SKIP LOCKED} statement, so several worker replicas can run without ever claiming the same row.
Holding a claim, the worker hands the run to the \texttt{RunDispatcher}, which marks the run \textit{running}, invokes the LangGraph pipeline through \texttt{advisory\_runner}, persists the final answer and the per-stage trace, and marks the session \textit{completed} (or appends a recovery message and marks it \textit{failed} on an exception).
The browser polls the session and trace endpoints until the run completes and then renders the answer together with the stage ladder.
Routing the run through a persisted queue rather than an in-process executor is what lets a run survive a restart of the web process and lets the application scale to more than one worker.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{seq_advisory_dispatch.png}
    \caption{Sequence of an advisory run dispatched to the background executor}
    \label{fig:seq-advisory}
\end{figure}

\begin{algorithm}[H]
\caption{Advisory run dispatch (non-blocking, durable queue)}
\label{alg:dispatch}
\SetKwProg{Fn}{Function}{:}{}
\KwIn{a student message $m$ on session $t$}
\Fn{\textsc{PostMessage}($t$, $m$) \tcp*[r]{request thread}}{
  $r \leftarrow$ \textsc{HandleUserMessage}($t$, $m$) \tcp*[r]{classify intent, update profile}\;
  \If{$r.\textit{shouldStartRun}$}{
    \textsc{EnqueueRun}($t$, $r.\textit{profileSnapshot}$, $r.\textit{dispatchArgs}$) \tcp*[r]{INSERT row, status \textit{queued}}\;
  }
  \Return $r$ \tcp*[r]{respond at once (NFR-04)}\;
}
\Fn{\textsc{WorkerLoop}()\tcp*[r]{background worker, separate process}}{
  \While{not stopped}{
    $c \leftarrow$ \textsc{ClaimNextQueued}() \tcp*[r]{UPDATE \ldots FOR UPDATE SKIP LOCKED}\;
    \eIf{$c = \textsc{None}$}{wait \textit{pollSeconds}\;}{
      \textsc{MarkRunning}($c.\textit{runId}$)\;
      \eIf{\textsc{InvokeGraph}($c$) succeeds $\to a$ \tcp*[r]{LangGraph pipeline + per-stage trace}}{
        \textsc{CompleteRun}($c.\textit{runId}$, $a$); \textsc{MarkSession}($c.\textit{token}$, \textit{completed})\;
      }{
        append recovery message; \textsc{MarkSession}($c.\textit{token}$, \textit{failed})\;
      }
    }
  }
}
\end{algorithm}

\textbf{Knowledge question answering.}
Figure~\ref{fig:seq-knowledge} traces a factual question, and Algorithm~\ref{alg:retrieval} gives the retrieval control flow that backs it.
The knowledge service embeds the question, runs a vector search over the knowledge chunks scoped to the requested school and topic, and additionally retrieves national (ministry) chunks with their own separate budget, because national regulations apply to every school; the two scopes keep independent top-$k$ limits so national chunks never crowd out the school's own, and the merged set is re-sorted by score.
If the best match falls below a confidence threshold, the service returns a ``no data'' result rather than calling the model, satisfying the honest-degradation behavior of UC-02.
Otherwise it asks the model to answer strictly from the retrieved passages, returns no-data again if the model produces no grounded answer, and resolves the cited sources from the passages the model says it used.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{seq_knowledge_qa.png}
    \caption{Sequence of a grounded knowledge question-answering request}
    \label{fig:seq-knowledge}
\end{figure}

\begin{algorithm}[H]
\caption{Scoped knowledge retrieval with confidence gating}
\label{alg:retrieval}
\KwIn{question $q$; optional school $s$ and topic $\theta$; thresholds \textit{minScore}, budgets \textit{topK}, \textit{nationalTopK}}
\KwOut{a grounded, cited answer, or a ``no data'' result}
$e \leftarrow \textsc{Embed}(q)$ \tcp*[r]{RETRIEVAL\_QUERY task type}\;
$C \leftarrow \textsc{VectorSearch}(e,\ \text{school}{=}s,\ \text{topic}{=}\theta,\ \text{limit}{=}\textit{topK})$\;
\If{$s \notin \{\textsc{None}, \textsc{National}\}$ \tcp*[r]{a school query also pulls ministry chunks}}{
  $N \leftarrow \{\,c \in \textsc{VectorSearch}(e,\ \text{school}{=}\textsc{National},\ \text{topic}{=}\theta,\ \text{limit}{=}\textit{nationalTopK}) : c.\textit{score} \geq \textit{minScore}\,\}$\;
  $C \leftarrow \textsc{SortByScore}(C \cup N)$\;
}
$\textit{conf} \leftarrow C \neq \emptyset\ ?\ C_1.\textit{score} : 0$\;
\If{$C = \emptyset$ \textbf{or} $\textit{conf} < \textit{minScore}$}{
  \Return \textsc{NoData}(\textit{conf}) \tcp*[r]{honest degradation, no model call (UC-02)}\;
}
$d \leftarrow \textsc{Generate}(q,\ C)$ \tcp*[r]{answer strictly from $C$; JSON \{answer, usedSourceIds\}}\;
\If{$d.\textit{answer}$ is empty}{\Return \textsc{NoData}(\textit{conf})\;}
\Return \textsc{Answer}($d.\textit{answer}$,\ \textsc{ResolveCitations}($C$,\ $d.\textit{usedSourceIds}$),\ \textit{conf})\;
\end{algorithm}

A design convention shared by every repository supports the testability requirement (NFR-05): each repository takes an injectable connection factory and performs its work over the PostgreSQL driver psycopg~\cite{psycopg} inside a \texttt{\_cursor} context manager that guarantees commit on success, rollback on exception, and connection cleanup in all cases.
The convention is implemented once in a shared helper (\texttt{services/db}) and reused by every repository, shown in Listing~\ref{lst:cursor}: the nested \texttt{try} blocks commit only when the caller asked for it, roll back and re-raise on any exception, and release the connection in a \texttt{finally} that runs on every path.
This both prevents leaked connections and lets tests substitute a connection factory bound to the isolated test database.

\begin{lstlisting}[language=Python,caption={The shared \texttt{cursor} context manager guaranteeing commit/rollback/cleanup, reused by every repository's \texttt{\_cursor}},label=lst:cursor]
@contextmanager
def cursor(connection_factory, commit: bool = False):
    conn = connection_factory()
    try:
        cur = conn.cursor()
        try:
            yield cur
            if commit:
                conn.commit()
        except Exception:
            conn.rollback()
            raise
        finally:
            cur.close()
    finally:
        release(conn)
\end{lstlisting}

\subsection{Database design}

The persistent state is one PostgreSQL schema built by nineteen numbered, idempotent migrations (\texttt{db/migrations/001--018}; two files share the \texttt{014} prefix, so the file count is one above the highest number).
Figure~\ref{fig:erd} shows the entity-relationship structure, grouped into four areas.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{erd_database.png}
    \caption{Entity-relationship diagram of the database schema}
    \label{fig:erd}
\end{figure}

The \textit{ingestion working store} (\texttt{source\_registry}, \texttt{discovered\_resources}, \texttt{raw\_documents}, \texttt{extracted\_facts}) records the provenance chain from a registered source to the raw facts extracted from a fetched document.
The \textit{canonical store} (\texttt{canonical\_admission\_records}, \texttt{cutoff\_records}) holds the normalized, query-ready admission data.
A design decision central to conflict awareness lives here: both canonical tables are unique \textit{per source} --- the uniqueness key includes the source \gls{URL} --- so two official publications that disagree about the same program coexist as two rows rather than overwriting each other.
The \textit{chat and tracing} area (\texttt{chat\_sessions}, \texttt{chat\_messages}, \texttt{chat\_advisory\_runs}, \texttt{advisory\_trace\_events}) stores anonymous sessions keyed by a random token, their transcripts, the advisory runs, and the per-stage trace events that drive the analysis panel.
The latest migration (\texttt{018\_advisory\_run\_queue}) turns \texttt{chat\_advisory\_runs} into a durable claim-based queue by adding claim-metadata columns (\texttt{worker\_id}, \texttt{claimed\_at}, \texttt{dispatch\_args\_json}) and a partial index over queued rows, which is the table behind the run-queue worker of Section~\ref{section:4.2}.
The \textit{knowledge corpus} area (\texttt{knowledge\_documents}, \texttt{knowledge\_chunks}, \texttt{program\_catalog\_embeddings}) holds the \gls{RAG} documents and their 768-dimensional embeddings, indexed with \gls{HNSW} for vector search.

\section{Application building}
\label{section:4.3}

\subsection{Libraries and tools}

Table~\ref{table:libraries} lists the principal libraries and their versions, taken from \texttt{requirements.txt}.

\begin{table}[H]
    \centering
    \caption{Principal libraries and tools, with versions}
    \label{table:libraries}
    \begin{tabular}{|p{3.6cm}|p{2.2cm}|p{7.5cm}|}
        \hline
        \textbf{Library} & \textbf{Version} & \textbf{Role} \\
        \hline
        Python & 3.12.0 & Implementation language \\
        \hline
        langgraph & 1.1.10 & Advisory pipeline state graph \\
        \hline
        langchain~\cite{langchain} & 1.2.17 & \gls{LLM} orchestration primitives \\
        \hline
        google-genai & 1.75.0 & Gemini \gls{API} client \\
        \hline
        pydantic & 2.13.4 & Typed data validation \\
        \hline
        psycopg2-binary & 2.9.12 & PostgreSQL driver \\
        \hline
        fastapi & 0.136.1 & Web framework \\
        \hline
        uvicorn & 0.46.0 & \gls{ASGI} server \\
        \hline
        jinja2 & 3.1.6 & \gls{HTML} templating \\
        \hline
        pdfplumber & 0.11.9 & \gls{PDF} text and table extraction \\
        \hline
        pymupdf & 1.27.2.3 & \gls{PDF} page rendering for \gls{OCR} \\
        \hline
        beautifulsoup4 & 4.14.3 & \gls{HTML} parsing \\
        \hline
        thefuzz & 0.22.1 & Fuzzy string matching in normalization \\
        \hline
        tenacity & 9.1.4 & Retry logic \\
        \hline
        pytest & 9.0.3 & Test framework \\
        \hline
    \end{tabular}
\end{table}

\subsection{Achievement}

The implemented system comprises 31{,}076 lines of Python across 376 tracked source files, of which 15{,}664 lines are tests.
By layer, ingestion is the largest at 6{,}695 lines, followed by the services layer at 6{,}089 lines.
The database schema is defined by nineteen idempotent migration files.

The data layer is populated from real official sources.
The canonical store holds 136 admission records for HUST and 20 for VNU-UET, together with 715 historical cutoff-score records (699 for HUST, 16 for VNU-UET) and 81 program-catalog embeddings used to resolve free-text major names.
The knowledge corpus for grounded question answering holds 18 documents and 406 embedded chunks, drawn from HUST, NEU, VNU-UET, and national \gls{MOET} regulation documents.
These figures are summarized in Table~\ref{table:achievement}.

\begin{table}[H]
    \centering
    \caption{Measured size of the implemented system and its data}
    \label{table:achievement}
    \begin{tabular}{|p{7.5cm}|r|}
        \hline
        \textbf{Metric} & \textbf{Value} \\
        \hline
        Python source lines (total) & 31{,}076 \\
        \hline
        Python source files (tracked) & 376 \\
        \hline
        Test lines & 15{,}664 \\
        \hline
        Database migrations (files) & 19 \\
        \hline
        Canonical admission records (HUST / VNU-UET) & 136 / 20 \\
        \hline
        Historical cutoff records (total) & 715 \\
        \hline
        Program-catalog embeddings & 81 \\
        \hline
        Knowledge documents / chunks & 18 / 406 \\
        \hline
        Automated tests & 1{,}011 \\
        \hline
    \end{tabular}
\end{table}

\subsection{Illustration of main functions}

The chat interface in Figure~\ref{fig:ui-landing} greets the student and invites a free-form description of their score, intended major, and preferred region.
As the conversation proceeds, the left pane fills with the collected profile and the right pane animates the six advisory stages for each run.
A completed consultation presents ranked program recommendations in the conversation pane, each with a fit assessment and, where the underlying sources disagree, an explicit note advising the student to verify against the latest official announcement.

\section{Testing}
\label{section:4.4}

The system is verified by an automated test suite that currently collects 1{,}011 tests across 167 test files, built with the pytest framework~\cite{pytest} and spanning unit tests of individual services, integration tests that exercise real \gls{SQL} paths, and end-to-end tests of the advisory and ingestion flows.

The defining feature of the test strategy is \textit{database isolation}.
A fixture in \texttt{tests/conftest.py} (\texttt{\_isolate\_test\_db}) redirects the entire suite to an automatically created \texttt{admission\_test} database, so integration and end-to-end tests can create, mutate, and drop data without ever touching the development database.
This makes the integration tests safe to run repeatedly and satisfies the testability requirement NFR-05.

The most recent full run on the isolated database completed in approximately 22 seconds with 1{,}009 tests passing, one skipped, and one erroring.
The skipped test is an end-to-end conflict-resolution scenario that requires an external dataset dump not committed to the repository.
The single error is an integration test of the trace endpoint that passes when run in isolation but errors only under full-suite execution, owing to a test-ordering interaction over shared database-connection-pool state; it is therefore a test-harness sequencing issue rather than a defect in the advisory or question-answering behavior.

Beyond the automated suite, the system's conversational behavior was evaluated against a catalogue of 25 edge cases documented in the project's requirements (\texttt{docs/edge-case.md}), covering short answers to pending questions, multi-slot messages, profile corrections, ambiguous major terms, conflicting source data, budget filtering, and conversation control.
In the most recent evaluation, 17 of the 25 cases were fully satisfied, 4 partially, and 4 not yet, up from 8 of 25 at an earlier baseline.
Table~\ref{table:edge-cases} summarizes the outcome.

\begin{table}[H]
    \centering
    \caption{Edge-case compliance evaluation (25 cases)}
    \label{table:edge-cases}
    \begin{tabular}{|p{2.6cm}|c|p{8.2cm}|}
        \hline
        \textbf{Outcome} & \textbf{Count} & \textbf{Representative cases} \\
        \hline
        Satisfied & 17 & Short pending-slot answers, multi-slot messages, score correction with re-ranking, eligibility gating, cutoff margin/volatility caveats, conflict disclosure, profile reset \\
        \hline
        Partial & 4 & Off-slot re-ask without e2e coverage, tuition shown but not scored, band-shift causality, partial data-uncertainty handling \\
        \hline
        Not yet & 4 & Removing a major, location and budget as structured soft preferences feeding retrieval \\
        \hline
    \end{tabular}
\end{table}

The four unmet cases share a single root cause: the profile model stores location and tuition as flat strings, and the reasoning stage does not yet consume them, so preferences that should soften or expand the search are not acted on.
This cluster is carried forward as the principal item of future work in Section~\ref{section:6.2}.

\section{Deployment}
\label{section:4.5}

The system is deployed with Docker Compose~\cite{docker}.
A single service definition runs the database from the \texttt{pgvector/pgvector:pg16} image, which provides PostgreSQL~16 with the pgvector extension preinstalled, exposed on the standard port with a persistent named volume and a health check.
Bringing up the database and waiting for its health check is a single command, after which a setup script (\texttt{db/setup\_db.py}) creates the \texttt{admission} database, applies the migrations in order, verifies the expected tables, and seeds the source registry.
The web application is then served by Uvicorn.
Ingestion is run separately through the \gls{CLI} to populate the canonical store and the knowledge corpus.
The whole bring-up is the short sequence of Listing~\ref{lst:deploy}.

\begin{lstlisting}[language={},caption={One-command database bring-up, schema setup, and application server (from \texttt{QUICKSTART.md})},label=lst:deploy]
docker compose up -d --wait db                 # pgvector/pgvector:pg16, waits for healthy
python -m db.setup_db                           # create DB, apply migrations, seed sources
python -m ingestion.main --school vnu_uet       # populate the canonical store (offline)
uvicorn web.app:build_app --factory --reload    # serve the chat application
\end{lstlisting}

This separation --- a containerized database, a one-command schema setup, an application server, and an offline ingestion \gls{CLI} --- keeps the runtime dependencies minimal (a Python environment and a Docker engine) while the idempotent migrations and upsert-based ingestion let the operator refresh school data at any time during the admission season, as required by NFR-02.

This chapter presented the system's layered architecture, the design of its core packages, the three-pane interface, the layer interactions behind the two central flows, the database schema, and the deployment, then evaluated the result against an automated suite of more than 1{,}000 tests and a 25-case edge-case catalogue.
The evaluation showed a system that is functionally substantial and well tested, with a clearly identified cluster of unmet preference-handling cases.
Chapter~\ref{chapter:SolutionAndContribution} steps back from the full system to present its three principal contributions in depth.

\end{document}
